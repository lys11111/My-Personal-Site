---
const navItems = [
  { href: '/', label: '首页' },
  { href: '/projects', label: '项目' },
  { href: '/learning', label: '学习' },
  { href: '/notes', label: '随笔' },
  { href: '/about', label: '关于' },
];

const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 border-b border-[--color-border]/80 bg-[--color-bg]/90 backdrop-blur-md">
  <nav class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
    <a href="/" class="font-serif text-lg font-semibold tracking-wide text-[--color-text] hover:text-[--color-accent]">
      龙文广
    </a>
    <ul class="flex items-center gap-1 sm:gap-2">
      {
        navItems.map((item) => {
          const isActive =
            item.href === '/'
              ? currentPath === '/'
              : currentPath.startsWith(item.href);
          return (
            <li>
              <a
                href={item.href}
                class:list={[
                  'rounded-md px-3 py-2 text-sm transition-colors',
                  isActive
                    ? 'text-[--color-accent]'
                    : 'text-[--color-muted] hover:text-[--color-text]',
                ]}
              >
                {item.label}
              </a>
            </li>
          );
        })
      }
    </ul>
  </nav>
</header>
